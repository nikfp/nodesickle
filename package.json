{
  "name": "trace",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --names \"TSC,NODE\" -c \"blue,green\" \"tsc --watch --preserveWatchOutput\" \"node --watch ./dist/index.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.22"
  }
}
